include base.pug

+base()
  +prose()
    +heading() The Black & White Collection
    +subheading() Monochrome artwork drawn with pen and paper or a mouse in Krita/MS Paint. Studies of contrast, shading, and value blending techniques.

  +img('imperfect', require(TINY('imperfect')), require(ART('imperfect')))
  +img('entropia', require(TINY('entropia')), require(ART('entropia')))
  +img('ruler', require(TINY('ruler')), require(ART('ruler')))
  +img('sketchedfaces', require(TINY('sketchedfaces')), require(ART('sketchedfaces')))
  +img('sketchedfaces2', require(TINY('sketchedfaces2')), require(ART('sketchedfaces2')))
  +img('hand', require(TINY('hand')), require(ART('hand')))
  +img('valueblending', require(TINY('valueblending')), require(ART('valueblending')))
  +img('blackpanther', require(TINY('blackpanther')), require(ART('blackpanther')))
  +img('scream', require(TINY('scream')), require(ART('scream')))
  +img('dehumanized', require(TINY('dehumanized')), require(ART('dehumanized')))
  +img('scrawnyman', require(TINY('scrawnyman')), require(ART('scrawnyman')))
  +img('mood', require(TINY('mood')), require(ART('mood')))
  +img('walter', require(TINY('walter')), require(OLD('walter')))
  +img('yee', require(TINY('yee')), require(OLD('yee')))
